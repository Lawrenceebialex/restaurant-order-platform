# Cloudflare Pages config
# Do NOT put a fake database_id here — it breaks deploy.
# Bind D1 in the dashboard instead:
#   Pages project → Settings → Functions → D1 database bindings
#   Variable name: DB
#   Database: restaurant-platform-db

name = "restaurant-order-platform"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."
